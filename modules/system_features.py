import streamlit as st


def show():
    """系統特色"""
    st.header("系統特色")
    
    features = [
        ("📱", "低門檻使用", "只需手機或電腦，即可完成姿勢檢測與健康數據整合。"),
        ("🎯", "個人化課表", "AI 根據個人體態、運動表現與恢復狀態動態調整。"),
        ("🔄", "多數據來源整合", "融合運動、InBody、健檢與穿戴裝置資訊，打造完整健康地圖。"),
        ("💡", "生成式 AI 行動建議", "將複雜數據轉化為直觀、可執行的運動與健康建議。"),
        ("📊", "可視化成果展示", "以圖表與雷達圖呈現訓練進展，提升使用者參與感。"),
    ]
    
    col1, col2, col3 = st.columns(3)
    cols = [col1, col2, col3]
    
    for idx, (icon, title, desc) in enumerate(features):
        with cols[idx % 3]:
            with st.container(border=True):
                st.write(f"## {icon}")
                st.write(f"### {title}")
                st.write(desc)

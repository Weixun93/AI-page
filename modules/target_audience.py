import streamlit as st


def show():
    """適用對象"""
    st.header("專為追求卓越的您而設計")
    
    targets = [
        ("一般健身者", "希望避免運動傷害，並獲得低成本、個人化的健身指導。"),
        ("專業運動員", "需要即時監控姿勢與恢復狀態，並降低昂貴檢測設備的依賴。"),
        ("健身教練與團隊", "作為輔助工具，提升教練在多名學員間的即時監控效率。"),
        ("健康管理需求者", "需結合健檢與日常數據，長期追蹤健康狀況並獲取生活建議。"),
    ]
    
    col1, col2 = st.columns(2)
    cols = [col1, col2]
    
    for idx, (audience, need) in enumerate(targets):
        with cols[idx % 2]:
            with st.container(border=True):
                st.write(f"### {audience}")
                st.write(f"**需求：** {need}")
    
    # CTA 最終呼籲
    st.divider()
    with st.container(border=True):
        st.write("## 立即加入 Motiv A.I.")
        st.write("""
這是一個能普及專業運動指導的解決方案。讓 AI 運動科學教練，成為全民健康生活的重要夥伴。
        """)
        
        col1, col2, col3 = st.columns(3)
        with col2:
            if st.button("🚀 立即註冊", key="register", use_container_width=True):
                st.success("感謝您的興趣！註冊頁面即將推出。")
    
    st.write("""
    ---
    支援 Web 瀏覽器 (Chrome / Edge)、App (iOS / Android)
    """)
